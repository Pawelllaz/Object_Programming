#include "pch.h"
#include <string.h>
#include <stdio.h>
#include <iostream>
#include <sstream>
#include "map_template.h"
#include "employee.h"

using namespace std;

template<typename T1, class Employee emp>
inline void map_template<id, emp>::Add(unsigned int i, class Employee emp)
{
	this->mapa.insert(i,emp);
}
template>typname T1, class Employee> 
map_template<T1, emp>::map_template(const class map_template& m)
{
	this->mapa = m;
}
